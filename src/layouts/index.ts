/**
 * Layout primitives — re-exported from Tamagui.
 *
 * Import layout containers from here so consumers have a single import source
 * and don't need to depend on Tamagui directly.
 *
 *   XStack — horizontal flex container (flexDirection: "row")
 *   YStack — vertical flex container (flexDirection: "column", default)
 *   ZStack — absolute-positioning stack
 *   Stack  — base flex container (alias for YStack)
 *   View   — unstyled Tamagui View
 */
export { XStack, YStack, ZStack, Stack, View } from "tamagui";
