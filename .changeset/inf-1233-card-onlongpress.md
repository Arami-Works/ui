---
"@aramiworks/ui": minor
---

Add `onLongPress` prop to Card atom. The Card now becomes interactive when either `onPress` or `onLongPress` is provided, wiring the handler through the inner Pressable.
