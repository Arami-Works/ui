---
"@aramiworks/ui": patch
---

Replace hardcoded radius and spacing values with design tokens across atoms and organisms — circles and pills now use `$full`, padding/gaps use semantic spacing tokens (`$xs`/`$sm`/`$md`/`$lg`/`$2xl`).
