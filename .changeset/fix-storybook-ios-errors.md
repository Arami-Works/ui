---
"@aramiworks/ui": patch
---

Fix iOS on-device Storybook errors: move storybook-sort import to preview (fixes deeperSort undefined), remove Tamagui config path from babel plugin (fixes parse error when extraction is disabled).
